package com.cardgame.model;

/** Actions a player can perform during the game. */
public enum MoveType {
    DRAW_FROM_DECK,
    DRAW_FROM_DISCARD,
    DISCARD_CARD,
    REARRANGE_CARDS,
    DECLARE_WIN
}

